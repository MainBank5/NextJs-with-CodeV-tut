//lets create an intercepted modal view that reflects in the url. the modal view is shown as well as url updated 
//ensuring the link shared opens to the modal component path
//naviagtging from the UI the intercepted f2 modal is shown however sharing the link/ refreshing the url
//the main f2 is shown

export default function F2() {
    return (
        <div>
          <h1> This is F2 page</h1>
        </div>
    )
  }